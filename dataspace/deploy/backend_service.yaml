apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: dataspace-dev
  labels:
    app: backend
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
